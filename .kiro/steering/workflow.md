# Workflow Rules

## Git / GitHub

After **every** code change, immediately run:

```
git add <file>; git commit -m "<message>"; git push origin main
```

Never leave changes uncommitted. Push to GitHub right after each edit.
